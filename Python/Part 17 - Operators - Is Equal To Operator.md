The == symbol is used to check if the first and second value have the same values.
```python
if 12 == 389:
    print("The first and second values are equal.")
```

The print function should execute.
```python
if 12 == 12:
    print("The first and second values are equal.")
```