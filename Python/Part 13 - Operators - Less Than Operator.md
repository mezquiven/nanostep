We can use < when comparing two values. Since the condition is true, the code inside if will execute and it is because 10 is less than 100.
```python
print("Experimenting with what you've learn will result to failures but they are fun to do and feels good when solved after mny tries.")

if 10 < 100:
    print("What projects are good to create?")

print("It is better to start with small projects that you create from scratch and no help from AI.)
```

We will not see the second string because this time 8361 is not less than 100.
```python
print("Experimenting with what you've learn will result to failures but they are fun to do and feels good when solved after mny tries.")

if 8361 < 100:
    print("What projects are good to create?")

print("It is better to start with small projects that you create from scratch and no help from AI.)
```